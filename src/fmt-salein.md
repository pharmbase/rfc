Допустимые значения для параметра Morion-Skynet-Tag в заголовке HTTP запроса:

```
	data.sale-inp.monthly.ua // Данные за месяц из Украины
	data.sale-inp.monthly.kz // Данные за месяц из Казахстана
	data.sale-inp.weekly.ua  // Данные за неделю из Украины
	data.sale-inp.daily.ua   // Данные за день из Украины
	data.sale-inp.daily.kz   // Данные за день из Казахстана
```

Пример:

```
	Morion-Skynet-Tag: data.sale-inp.monthly.ua
```

Формат приема-передачи данных:

```
{
	"meta": {
		"id": "string",         // Идентификатор торговой точки (внешний код)
		"name": "string",       // Название торговой точки
		"head": "string",       // Название торговой сети (юр. лица)
		"addr": "string",       // Адрес торговой точки
		"edrp": "string",       // ЕДРПОУ (ОКПО)
		"time": "string",       // Время отправки данных строкой ISO 8601 (RFC3339) 2015-02-17T01:15:25+02:00
		"time_range": ["", ""], // Временной диапазон строкой ISO 8601 (RFC3339) 2015-02-17T01:15:25+02:00

		"link": { // Объект добавляется сервисом распознавания (если связь установлена)
			"id": "string", // ID_ADDRESS
			"sha": "string" // SHA1(len1024("name/head: addr"))
		},

		"mark": { // Объект добавляется сервисом распознавания (всегда)
			"key": "string",  // Ключ исходного отправителя
			"tag": "string",  // Значение Morion-Skynet-Tag
			"sha": "string",  // SHA1(body[{},...])
			"time": "string"  // Время приема данных строкой ISO 8601 (RFC3339) 2015-02-17T01:15:25+02:00
		},

		"test": { // Объект добавляется сервисом проверки данных (всегда)
			"code": 0,            // Код статуса результата проверки (int)
			"text": "string",     // Текстовое сообщение результата проверки
			"time": "string",     // Время проверки данных строкой ISO 8601 (RFC3339) 2015-02-17T01:15:25+02:00
			"qty_lines": 0,       // Количество строк (товарных позиций всего) (int)
			"qty_links": 0,       // Количество связей (распознанных товаров) (int)
			"qty_nulls": 0,       // Количество пустых (нераспознанных товаров) (int)
			"qty_fails": 0,       // Количество неудач при проверке позиций (int)
			"pct_fails": 0.0,     // Процент неудач в связанных товарах (float)
			"pct_fails_max": 0.0, // Процент неудач в связанных товарах максимально допустимый (float)
			"pct_nulls": 0.0,     // Процент нераспознанных товаров (float)
			"pct_nulls_max": 0.0  // Процент нераспознанных товаров максимально допустимый (float)
		}

	},
	"data": [{
		"id": "string",  // Идентификатор товара в торговой точке (внешний код)
		"gds": "string", // Товар, полное наименование препарата + производитель (через пробел)
		"qty": 0.0,      // Количество прихода (float)
		"prc": 0.0,      // Цена прихода (float)
		"bln": 0.0,      // Остаток на конец периода (float)

		"supp": { // Информация о поставщике
			"name": "string",  // Название поставщика
			"edrp": "string",  // ЕДРПОУ (ОКПО)

			"link": { // Объект добавляется сервером распознавания (если связь установлена)
				"id": "string", // ID_ADDRESS
				"sha": "string" // SHA1(name)
			}
		}
		
		"link": { // Объект добавляется сервером распознавания (если связь установлена)
			"id": "string", // ID_DRUG
			"sha": "string" // SHA1(gds)
		},

		"fail": { // Объект добавляется сервисом проверки данных (если облом)
			"code": 0,      // Код статуса результата проверки (int)
			"text": 0.0,    // Текстовое сообщение результата проверки
			"coef": 0.0,    // Коэффициент (множитель) эксперта
			"prc_fix": 0.0, // Цена фиксированная экспертом ("пропускная")
			"prc_avg": 0.0, // Цена усредненная за прошлый период
			"qty_min": 0.0, // Количество минимальное
			"qty_max": 0.0  // Количество максимальное
		}
	}]
}
```
